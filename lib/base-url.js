// Canonical public base URL for docs and machine-readable metadata.
// Override with PUBLIC_API_BASE_URL if the canonical hostname ever changes.
export const PUBLIC_API_BASE_URL =
  process.env.PUBLIC_API_BASE_URL ?? "https://api.santosautomation.com";
